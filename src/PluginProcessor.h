    std::atomic<int> trimInSample { 0 }; // existing member
    std::atomic<int> trimOutSample { 0 }; // existing member
    std::atomic<int> trimPreference { 0 }; // new member
    std::atomic<int> trimRegionStart { 0 }; // new member
    std::atomic<int> trimRegionEnd { 0 }; // new member
